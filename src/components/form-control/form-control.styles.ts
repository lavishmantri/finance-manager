import { createStyles, MantineTheme } from '@mantine/core';

export const useFormControlStyles = createStyles((theme: MantineTheme) => {
  return {
    formControl: {
      width: '200px',
      marginTop: '12px',
    },
  };
});
